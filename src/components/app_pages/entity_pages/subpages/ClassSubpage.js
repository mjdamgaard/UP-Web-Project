import {useState, useMemo, useContext} from "react";
import {useDispatch} from "../../../../hooks/useDispatch";

/* Placeholders */
const TabHeader = () => <template></template>;



export const ClassSubpage = (props) => {
  return (
    <div>
      Class subpage... {JSON.stringify(props)}
    </div>
  );
};

