// IdentPatt is a production consisting of several ident patterns, namely
// since they have different (Hungarian noation) prefixes (which should
// btw be defined in a module, that this module imports from).


export const reqWS = "(\s+)";
export const optWS = "(\s*)";
